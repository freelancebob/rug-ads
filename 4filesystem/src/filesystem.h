#ifndef FILESYSTEM_H
#define FILESYSTEM_H

// TODO: Rename this data structure and all references to it, and adapt its contents to your needs.
typedef struct DataStructure {
    int a;
    int b;
    int c;
} DataStructure;

#endif
